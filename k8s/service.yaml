apiVersion: v1
kind: Service
metadata:
  labels:
    app: quiz-test-app
  name: quiz-test-app
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  selector:
    app: quiz-test-app